# Local Development Environment Variables
# Copy this file to .env and fill in your values

# Database (local development uses SQLite)
DATABASE_URL="file:./prisma/dev.db"

# Better Auth Configuration (replaces NextAuth)
BETTER_AUTH_URL="http://localhost:3000"
BETTER_AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Google OAuth
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Production Note:
# For Cloudflare deployment, set these via Cloudflare Pages Dashboard:
# Pages > miniorg > Settings > Environment variables
# 
# Required variables:
# - BETTER_AUTH_SECRET (Encrypted)
# - BETTER_AUTH_URL (e.g., https://miniorg.pages.dev)
# - GOOGLE_CLIENT_ID (Encrypted)
# - GOOGLE_CLIENT_SECRET (Encrypted)
#
# D1 Database binding is configured in wrangler.toml
