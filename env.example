# Local Development Environment Variables
# Copy this file to .env and fill in your values

# Database (local development uses SQLite)
DATABASE_URL="file:./prisma/dev.db"

# NextAuth v5 Configuration
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"

# Auth URL - Update this for your domain in production
AUTH_URL="http://localhost:3000"

# Google OAuth
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Production Deployment on Cloudflare Workers:
# Set these secrets via wrangler CLI:
# 
# wrangler secret put AUTH_SECRET
# wrangler secret put GOOGLE_CLIENT_ID
# wrangler secret put GOOGLE_CLIENT_SECRET
#
# Or via Cloudflare Dashboard:
# Workers & Pages > Your Worker > Settings > Variables
# 
# Required secrets:
# - AUTH_SECRET (NextAuth v5 uses this name)
# - AUTH_URL (e.g., https://miniorg.your-domain.workers.dev)
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET
#
# Note: D1 Database binding (DB) is configured in wrangler.toml
